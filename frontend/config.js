// Set this to your backend's public URL after deploying to Render.
// Leave as null to auto-detect: localhost in dev, same-origin in prod.
// Example: window.PEBLO_API_URL = 'https://peblo-notes-api.onrender.com';
window.PEBLO_API_URL = null;
